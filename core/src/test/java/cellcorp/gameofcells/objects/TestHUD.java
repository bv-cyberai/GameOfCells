package cellcorp.gameofcells.objects;

import org.junit.jupiter.api.Test;

public class TestHUD {

    /**
     * Update Tester
     * 
     * Tests that hud updates properly.
     */
    @Test
    public void testUpdate() {
        // var fakeAssetManager = Mockito.mock(AssetManager.class);
        
        // Cell fakeCell = Mockito.mock(Cell.class);
        // // Define return values for mocked methods
        // Mockito.when(fakeCell.getCellHealth()).thenReturn(100);
        // Mockito.when(fakeCell.getCellATP()).thenReturn(0);

        // var fakeShapeRenderer = Mockito.mock()
        
        
        // HUD hud = new HUD(fakeAssetManager,);
        // hud.update(1f,fakeCell.getCellHealth(),fakeCell.getCellATP()); // simulate 1 second has passed

        // assertEquals("Timer: 1", hud.getTimerString());
        // assertEquals("HEALTH: 100/100", hud.getCellHealthString());
        // assertEquals("ATP: 0", hud.getAtpString());
    }

}
